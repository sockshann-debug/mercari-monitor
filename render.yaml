services:
  - type: worker
    name: mercari-monitor
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python mercari_monitor.py
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
      - key: TELEGRAM_CHAT_ID
        sync: false
